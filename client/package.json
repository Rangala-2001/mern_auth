{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "classnames": "^2.2.6",
    "jwt-decode": "^2.2.0",
    "react": "^18.0.0",  // Updated React version
    "react-dom": "^18.0.0",  // Updated React DOM version
    "react-redux": "^5.1.1",
    "react-router-dom": "^5.1.1",  // Updated to the latest compatible version
    "react-scripts": "5.0.0",  // Updated to the latest stable version
    "redux": "^4.0.1",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "proxy": "http://backend:5000",  // Updated to use the backend container in Docker
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
